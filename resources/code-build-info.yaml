application-platform:
  - name : java

code_build_steps:
  - name: java
    commands:
      - "mvn -v"
      - "echo mvn clean package"
      - "mvn clean install"
      - "echo java -jar target/spring-boot-web.jar "

  - name: python
    commands:
      - "python --version"
      - ls -l
      - pwd

  - name: .net
    commands:
      - "python --version"
      - ls -l
      - pwd

  - name: nodejs
    commands:
        - "echo npm install cross-env"
        - ls -l
        - pwd
