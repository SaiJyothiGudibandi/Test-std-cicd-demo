application-platform:
  - name : java

code_build_steps:
  - name: java
    commands:
      - "echo mvn -v"

  - name: python
    commands:
      - "python --version"
      - ls -l
      - pwd

  - name: .net
    commands:
      - "python --version"
      - ls -l
      - pwd

  - name: nodejs
    commands:
        - "echo npm install cross-env"
        - ls -l
        - pwd